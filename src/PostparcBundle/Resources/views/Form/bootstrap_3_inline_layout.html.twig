{% use "bootstrap_3_layout.html.twig" %}

{% block form_start -%}
    {% set attr = attr|merge({class: (attr.class|default('') ~ ' form form-inline')|trim}) %}
    {{- parent() -}}
{%- endblock form_start %}
